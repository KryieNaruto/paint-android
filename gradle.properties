# JVM 参数：避免 headless 构建 OOM；Gradle 8.x 默认按 CPU 数派 worker。
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# AndroidX 建议开启。
android.useAndroidX=true

# 跳过依赖校验（加速 headless 构建）。
android.nonTransitiveRClass=true
